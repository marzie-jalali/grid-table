import styled from "@emotion/styled";
import { type AppThemeModel } from "nakit";

export const NestedTableContainer = styled.div<{ theme?: AppThemeModel }>(
  ({ theme }) => {
    return {};
  },
);

export const NestedHeaderTable = styled.div<{ theme?: AppThemeModel }>(
  ({ theme }) => {
    return {};
  },
);
